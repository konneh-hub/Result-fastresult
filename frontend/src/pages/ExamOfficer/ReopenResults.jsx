import React from 'react';
import PlaceholderPage from '../../components/PlaceholderPage';

const ReopenResults = () => (
  <PlaceholderPage
    title="Reopen Results"
    description="Reopen locked results for corrections if authorized."
  />
);

export default ReopenResults;
