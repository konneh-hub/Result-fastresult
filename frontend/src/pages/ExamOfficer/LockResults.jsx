import React from 'react';
import PlaceholderPage from '../../components/PlaceholderPage';

const LockResults = () => (
  <PlaceholderPage
    title="Lock Results"
    description="Lock final results to prevent further changes."
  />
);

export default LockResults;
