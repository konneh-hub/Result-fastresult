import React from 'react';
import PlaceholderPage from '../../components/PlaceholderPage';

const GpaCalculator = () => (
  <PlaceholderPage
    title="GPA Calculator"
    description="Calculate estimated GPA based on course scores."
  />
);

export default GpaCalculator;
